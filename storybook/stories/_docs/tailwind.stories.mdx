import { Meta, Story, Preview, Canvas } from '@storybook/addon-docs/blocks'
import { theme, variants } from '../../../tailwind.config.js'

<Meta title="Appendix/TailwindCSS" parameters={{ viewMode: 'docs' }} />

# TailwindCSS

This design system uses [TailwindCSS](https://tailwindcss.com/). For more information on how to use Tailwind, check the [Tailwind Docs](https://tailwindcss.com/docs).

## Best Practices

Overall, we aim to use inline classes whenever possible. When not possible, we use extracted classes with tailwind's `@apply` directive.

## TailwindCSS Plugins

We also use the `@tailwindcss/forms` plugin. View the [docs](https://github.com/tailwindlabs/tailwindcss-forms)

## Tailwind Configuration

You can view our entire configuration below:

### Theme

<code language="json">{JSON.stringify(theme, null, 2)}</code>

### Variants

<code language="json">{JSON.stringify(variants, null, 2)}</code>
