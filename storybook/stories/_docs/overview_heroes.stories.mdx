import { Meta } from '@storybook/addon-docs'
import { ReactDocsComponentList } from './ReactDocsComponentList'

<Meta
  title="Components/Blocks/Heroes/Overview"
  parameters={{ viewMode: 'docs' }}
/>

# Hero Blocks

Hero components are reserved for usage at the top of the page. There should only be one hero per page. All hero blocks include page layout assumptions and use a max width of `max-w-screen-3xl`.

<ReactDocsComponentList
  components={[
    {
      heading: 'Small',
      description:
        'Includes a text overlay. Height of 200px (flexible) on small screens; fixed at 360px on larger screens.',
      storyId: 'components-blocks-heroes-small--default',
      fullWidth: true,
      fullCanvas: true,
    },
    {
      heading: 'Medium',
      description:
        'Includes a text overlay. Height of 400px (flexible) on small screens; fixed at 600px on larger screens.',
      storyId: 'components-blocks-heroes-medium--default',
      fullWidth: true,
      fullCanvas: true,
    },
    {
      heading: 'Large',
      description:
        'Includes a large text overlay that works best with background images that have a focal point on the right side of the image.',
      storyId: 'components-blocks-heroes-large--hero',
      fullWidth: true,
      fullCanvas: true,
    },
    {
      heading: 'Media Only',
      description:
        'No text overlay, just an image or video with a caption below.',
      storyId: 'components-blocks-heroes-media-only--default',
      fullWidth: true,
      fullCanvas: true,
    },
  ]}
/>
